{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "mix_task",
      "name": "mix (Default task)",
      "request": "launch",
      "taskArgs": ["lib/backend.ex"],
      "projectDir": "${workspaceRoot}"
    }
  ]
}
